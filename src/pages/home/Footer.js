import React from 'react';
import Nav from './Nav';

export default (props) => {
  return (
    <footer className="footer">
      <p>This is the page's footer</p>
      <input placeholder="sign up to newsletter" type="text" />
    </footer>
  )
}