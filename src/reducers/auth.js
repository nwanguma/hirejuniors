const demoAuth = {
  uid: 'ahdhklehlsahlakl',
  isDeveloper: false,
  isRecruiter: false
}

const auth = (state = demoAuth, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default auth;