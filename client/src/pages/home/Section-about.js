import React from 'react';

export default (props) => {
  return (
    <section className="section-companies">
      <p>Find out everything you need to know about jobs for junior developers</p>
    </section>
  )
}