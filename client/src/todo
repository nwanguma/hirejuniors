Implement backend validation
Set up errors reducer to contain error messages from backend validation
Set up login and authentication
Store token in local storage
Configure axios requests to send along the token
Configure redirect to profile page if user is register/if user is logged in without profile
Configure redirect to dashboard if user tries to sign in or sign up if they are already logged in
Set up ability to create jobs for recruiters via a post job from
Configure blog page to contain all articles
Configure jobs to contain all jobs
Set up talent page to display all developer profiles which contains filters for searching or sorting developers
Configure homepage to contain snippets for jobs and articles